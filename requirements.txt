# Pinned to the versions this pipeline was developed and validated against
# (Python 3.11). ffmpeg must also be on PATH -- used by sync_audio.py.
mediapipe==0.10.35
opencv-python==4.13.0
numpy==2.4.6
scipy==1.17.1
matplotlib==3.11.0
